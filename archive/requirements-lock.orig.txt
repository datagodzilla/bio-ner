# Original freeze (non-portable) was removed because it contained local file:// build references
# If you need the original dump, retrieve it from the git history (this repository's earlier commits)
# Example: `git show <old-commit>:requirements-lock.txt > archive/requirements-lock.orig.txt`
